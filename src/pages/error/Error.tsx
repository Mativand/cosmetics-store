import {NavLink} from "react-router-dom";

const Error = () => {
    return (
        <div>
            Error
            <NavLink to={'/'}>Home</NavLink>
        </div>
    );
};

export default Error;